{
  "git": {
    "deploymentEnabled": {
      "main": true,
      "dependabot/**": false
    }
  },
  "github": {
    "autoAlias": false
  },
  "buildCommand": "pnpm install && cd packages/database && pnpm prisma generate --schema=./prisma/schema.prisma && cd ../.. && pnpm run build",
  "installCommand": "pnpm install"
}
